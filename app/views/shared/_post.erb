<div class="border">
  <div class="user_content">
  <h3><%= link_to post.title, user_post_path(@user, post) %> #<% post.id %> </h3>
  <p><%= "#{post.text.slice(0, 100)}..." %></p>
  <p class="post-count">Comments: <%= post.comments_counter || 0 %>,Likes: <%= post.likes_counter || 0 %></p>
  </div>
</div>