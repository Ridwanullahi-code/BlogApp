<div class="border border-gray-200 flex-1 mx-5">
  <div class="flex flex-col gap-3 p-3 px-5">
  <h3 class="text-2xl font-bold hover:text-blue-700 active:text-blue-800 duration-75"><%= link_to post.title, user_post_path(@user, post) %> #<% post.id %> </h3>
  <p><%= "#{post.text.slice(0, 100)}..." %></p>
  <p class="text-right">Comments: <%= post.comments_counter || 0 %>,Likes: <%= post.likes_counter || 0 %></p>
  </div>
</div>
